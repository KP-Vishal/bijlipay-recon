{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn api.routes:app --host 0.0.0.0 --port 8000",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
```

**STEP 5 — Click "Commit changes"**

Railway will automatically redeploy. Watch the service — it should go from **Online** to **Deploying** and back to **Online**.

**STEP 6 — Test it:**
```
https://web-production-c8a69.up.railway.app/health
